# REST (API CRUD Spring) - rutas relativas, Vite las envía por proxy a http://localhost:8080
VITE_API_BASE_URL=/api
VITE_AUTH_BASE_URL=/auth
VITE_USE_MOCK=false

# Tiempo real: STOMP sobre WebSocket en el mismo backend Spring (endpoint /ws-blueprints)
VITE_STOMP_BASE=http://localhost:8080
